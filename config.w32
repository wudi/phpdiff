// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("phpdiff", "for phpdiff support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("phpdiff", "enable phpdiff support", "no");

if (PHP_PHPDIFF != "no") {
	EXTENSION("phpdiff", "phpdiff.c");
}

